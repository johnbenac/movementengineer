{
  "name": "movementengineer",
  "version": "1.0.0",
  "description": "Movement Engineer is a client-side tool for exploring, authoring, and comparing structured data about social movements using the Markdown v2.3 dataset format.",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "check:no-appjs": "node scripts/check-no-appjs.mjs",
    "build": "npm run check:no-appjs && node scripts/build-dist.mjs",
    "test": "npm run check:no-appjs && node markdown-dataset-loader.test.js && node view-models.test.js && node comparison-services.test.js && npm run test:unit && npm run test:e2e",
    "postinstall": "playwright install --with-deps",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:legacyfree": "ME_E2E_ENTRY=/legacy-free.html playwright test",
    "test:legacyfree": "npm run test:unit && npm run test:e2e:legacyfree"
  },
  "keywords": [],
  "author": "",
  "type": "commonjs",
  "dependencies": {
    "js-yaml": "^4.1.0",
    "jszip": "^3.10.1",
    "undici": "^6.21.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "jsdom": "^24.1.0",
    "vitest": "^1.6.0"
  }
}
